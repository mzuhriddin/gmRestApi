server:
  port: 80
  error:
    include-message: always
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gm_app
    username: postgres
    password: 2002
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true